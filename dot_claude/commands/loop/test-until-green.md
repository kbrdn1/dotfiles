---
description: "Loop auto-cadencé — relance `npm test` et corrige jusqu'à ce que tous les tests passent (exit 0), max 10 itérations"
---

Invoke the `loop:test-until-green` skill via the Skill tool to run this self-paced loop.

Pass along any user arguments: $ARGUMENTS
