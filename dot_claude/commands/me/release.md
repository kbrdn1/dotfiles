---
description: "Publie une version : changelog → bump → merge dev → main → tag (après le merge) → release GitHub → propagation"
argument-hint: "[X.Y.Z | patch | minor | major]"
---

Invoke the `me:release` skill via the Skill tool to handle this request.

Pass along the user's arguments: $ARGUMENTS
