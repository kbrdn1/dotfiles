---
description: "Transforme un repo en carte d'architecture isométrique interactive (HTML autonome, thème Claude Dark) publiée en artifact — blocs aux vraies LOC, flux animés, go-inside, trace pas-à-pas"
argument-hint: "[repo / chemin à cartographier + angle voulu]"
---

Invoke the `me:codebase-visualizer` skill via the Skill tool to handle this request.

Pass along the user's arguments: $ARGUMENTS
