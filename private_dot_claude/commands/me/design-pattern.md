---
description: "Design patterns — select : choisir le patron adapté à un besoin (ou aucun) · analyze : lire du code existant, smells + SOLID + refactorings (read-only)"
argument-hint: "<select|analyze> <besoin ou chemins…> [--base <ref>] [--report <chemin>] [--lang <langage>]"
---

Invoke the `me:design-pattern` skill via the Skill tool to handle this request.

Pass along the user's arguments: $ARGUMENTS
