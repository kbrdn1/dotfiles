---
description: "Génère une bannière promo slim (presque one-line, PNG dark + light) au thème Claude Dark — wordmark, tagline, install, version, à embarquer dans un README"
argument-hint: "[projet à promouvoir en bandeau compact]"
---

Invoke the `me:banner-slim` skill via the Skill tool to handle this request.

Pass along the user's arguments: $ARGUMENTS
