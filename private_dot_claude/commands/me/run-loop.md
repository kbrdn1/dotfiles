---
description: "Lance une boucle auto-cadencée (self-pace) jusqu'à une condition de sortie observable — route vers la bonne skill loop:<name>"
argument-hint: "<nom-du-loop | description de la condition de sortie>"
---

Invoke the `me:run-loop` skill via the Skill tool to handle this request.

Pass along the user's arguments: $ARGUMENTS
