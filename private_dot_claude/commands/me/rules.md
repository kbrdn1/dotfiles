---
description: "Crée et fait évoluer les <repo>/.claude/rules/ path-scoped selon le stack du projet"
argument-hint: "init | update"
---

Invoke the `me:rules` skill via the Skill tool to handle this request.

Pass along the user's arguments: $ARGUMENTS
